import ChatClient from './ChatClient';

export const metadata = {
  title: 'IA Assistant | CRM Mateus',
  description: 'Inteligência artificial para sua agência',
};

export default function IAPage() {
  return <ChatClient />;
}

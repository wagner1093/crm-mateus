import ServicosClient from './ServicosClient';

export const metadata = {
  title: 'Serviços | CRM Mateus',
  description: 'Catálogo de serviços da agência',
};

export default function ServicosPage() {
  return <ServicosClient />;
}
